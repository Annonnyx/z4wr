export default {
  name: 'banner',
  description: 'Générer ou afficher une bannière (placeholder)',
  async execute({ client, message, args }) {
    await message.channel.send('Commande banner temporaire — non implémentée.');
  }
};
