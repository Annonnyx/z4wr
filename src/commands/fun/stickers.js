export default {
  name: 'stickers',
  description: 'Placeholder: créer un sticker depuis un autre serveur (non implémenté)',
  async execute({ client, message }) {
    await message.channel.send('Création de stickers non implémentée via API publique ici.');
  }
};
